# Pinned to the versions these figures + pipeline were last verified with
# (Python 3.12). Rendering the figures needs only the first block (CPU-only).
# The from-scratch GPU pipeline additionally needs the second block.

# --- figure rendering (CPU) ---
numpy==2.2.6
scipy==1.16.3
matplotlib==3.10.8
adjustText==1.3.0
torch==2.10.0          # only used to load the .pt activation tensors

# --- from-scratch data generation (GPU); not needed just to render figures ---
transformers==4.57.6
accelerate==1.13.0
vllm==0.19.0
huggingface_hub>=0.36
